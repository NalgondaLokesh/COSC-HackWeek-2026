# Digital Business Card Generator

Simple static project for generating digital business cards.

## Files
- `index.html` — main page for the generator.

## How to use
Open `index.html` in a browser to view and use the generator.

## Git
This repository contains a single-page static site. To push to GitHub:

1. Create a GitHub repo and add it as a remote, for example:

```
git remote add origin https://github.com/<username>/<repo>.git
git push -u origin main
```

Replace `main` with your default branch name if different.

---
Generated and added by a helper script.
